# Signed bundle gate

v0.3 signed-bundle support requires:

- signing replay-bundle.json
- emitting signed-bundle-envelope.json
- verifying valid signature envelopes
- failing verification after bundle tamper
- preserving no truth/safety/authorization claims
